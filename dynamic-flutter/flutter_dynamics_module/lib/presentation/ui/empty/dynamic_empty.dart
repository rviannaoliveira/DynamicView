import 'package:flutter/material.dart';

class DynamicEmpty extends StatelessWidget {

  const DynamicEmpty({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
